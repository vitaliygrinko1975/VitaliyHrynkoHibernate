version: "3.9"

services:
  db:
    image: library/mysql:latest
    volumes:
      - db_data:/var/lib/mysql
    ports:
      - "3306:3306"
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root

volumes:
  db_data: {}